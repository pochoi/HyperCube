% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/hypercubeEst.R
\name{hypercubeEst}
\alias{hypercubeEst}
\title{Estimation on linear Mode for Hypercube}
\usage{
hypercubeEst(X, y, V, ...)
}
\arguments{
\item{X}{design matrix, or data-incidence matrix (as described Beran (2014))}

\item{y}{response, or observation (as described Beran (2014))}

\item{V}{sysmmetric matrix whose eigenvalues all lie in [0,1] (as in equation (1.3) in Beran (2014))}

\item{...}{other optional arguments}
}
\value{
The function \code{hypercubeEst} returns a list containing the following:
\describe{
  \item{coefficients}{Estimated coefficents \eqn{\beta} under hypercube estimator}
  \item{fitted.values}{Fitted values (\eqn{\eta}) under hypercube estimator}
  \item{residuals}{residuals, \eqn{y - \eta}}
  \item{V}{Symmtric matrix with all eigenvalues lie in [0,1].
  The matrix V used the hypercube estimator.
  (as in equation (1.3) in Beran (2014))}
}
}
\description{
This function calculates the hypercube estimates. Users should use \code{\link{hypercube}} which is a better user interface.
}
\examples{
## The use of the function \\code{hypercubeEst}.
mf <- model.frame(weight ~ mother:infant -1, data = litter)
X <- model.matrix(attr(mf, "terms"), mf)
y <- model.response(mf)
litter.proj <- projectCreate( ~ mother:infant -1, data = litter)
V <- projectWeight(litter.proj, weights = c(1,1,1,0))
hcmod <- hypercubeEst(X, y, V)
hcmod$coefficients
}
\references{
Beran, Rudolf. "Hypercube estimators: Penalized least squares, submodel selection, and numerical stability." Computational Statistics & Data Analysis 71 (2014): 654-666.
}
\seealso{
\code{\link{hypercube}} is the user interfaces of hypercube estimators. The matrix V can be constructed using the function \code{\link{projectCreate}}.
}

